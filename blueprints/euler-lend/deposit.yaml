protocol: "euler-lend"

inputs:
  asset_address:
    type: "address"
  vault_address:
    type: "address"
  caliber_address:
    type: "address"

actions:
  deposit:
    calls:
      - description: "Approve underlying asset to the Euler Earn vault"
        target: "${inputs.asset_address}"
        selector: "approve(address,uint256)"
        parameters:
          - type: "address"
            value: "${inputs.vault_address}"
          - type: "uint256"
            value: "${input_slots.amount_to_deposit}"

      - description: "Deposit assets into Euler Earn vault and receive vault shares"
        target: "${inputs.vault_address}"
        selector: "deposit(uint256,address)"
        parameters:
          - type: "uint256"
            value: "${input_slots.amount_to_deposit}"
          - type: "address"
            value: "${inputs.caliber_address}"

    input_slots:
      amount_to_deposit:
        type: "uint256"
        description: "The amount of underlying assets to deposit into the Euler Earn vault"
