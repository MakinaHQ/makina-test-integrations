# AaveV3 sUSDe Supply with E-Mode activation/deactivation.
#
# Expected caliber vars:
#   label:         string   — position label
#   asset_address: address  — sUSDe token address
#   aave_token:    address  — aToken address for sUSDe
#
# Expected config:
#   caliber_address
#   aavev3_core_instance

# 1/5 - Activate sUSDe Stablecoins E-Mode (category 2)
- is_debt: false
  instruction_type: "MANAGEMENT"
  affected_tokens: []
  instruction:
    label: "sUSDe Stablecoins E-Mode - Activate"
    path: "../../../blueprints/aave/deposit.yaml:set_e_mode"
    inputs:
      aave_pool_instance:
        type: "address"
        value: "${config.aavev3_core_instance}"
      e_mode:
        type: "uint8"
        value: "2"

# 2/5 - Supply sUSDe as collateral
- is_debt: false
  instruction_type: "MANAGEMENT"
  affected_tokens:
    - ${position.asset_address}
  instruction:
    label: ${position.label}
    path: "../../../blueprints/aave/deposit.yaml:add_collateral"
    inputs:
      on_behalf_of:
        type: "address"
        value: ${config.caliber_address}
      asset_address:
        type: "address"
        value: ${position.asset_address}
      aave_pool_instance:
        type: "address"
        value: "${config.aavev3_core_instance}"

# 3/5 - Withdraw sUSDe collateral
- is_debt: false
  instruction_type: "MANAGEMENT"
  affected_tokens:
    - ${position.asset_address}
  instruction:
    label: ${position.label}
    path: "../../../blueprints/aave/withdraw.yaml:withdraw_collateral"
    inputs:
      to:
        type: "address"
        value: ${config.caliber_address}
      asset_address:
        type: "address"
        value: ${position.asset_address}
      aave_pool_instance:
        type: "address"
        value: "${config.aavev3_core_instance}"

# 4/5 - Deactivate E-Mode (category 0)
- is_debt: false
  instruction_type: "MANAGEMENT"
  affected_tokens: []
  instruction:
    label: "sUSDe Stablecoins E-Mode - Deactivate"
    path: "../../../blueprints/aave/deposit.yaml:set_e_mode"
    inputs:
      aave_pool_instance:
        type: "address"
        value: "${config.aavev3_core_instance}"
      e_mode:
        type: "uint8"
        value: "0"

# 5/5 - Accounting
- is_debt: false
  instruction_type: "ACCOUNTING"
  affected_tokens:
    - ${position.asset_address}
  instruction:
    label: ${position.label}
    path: "../../../blueprints/aave/account.yaml:account"
    inputs:
      aave_token:
        type: "address"
        value: ${position.aave_token}
      account:
        type: "address"
        value: "${config.caliber_address}"
